# React-SimpleRegister
<p>Simple Register System to learn a little about React.
<p>The API used in this system was a simple JSON generated by Mocky and the register effectively doesn't work, but I generated two different JSON, one to method GET and other to method POST. 
